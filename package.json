{
  "name": "particles-image",
  "description": "Deploy a parcel app to GitHub pages",
  "homepage": "https://isengupt.github.io/particles-image",
  "scripts": {
    "start": "parcel index.html",
    "predeploy": "rm -rf dist && parcel build index.html --public-url /particles-image",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "dat.gui": "^0.7.7",
    "gsap": "^3.5.1",
    "three": "^0.123.0",
    "three-orbit-controls": "^82.1.0"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "glslify-bundle": "^5.1.1",
    "glslify-deps": "^1.3.2",
    "parcel": "^1.12.4"
  }
}
